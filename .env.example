# 金数据(Jinshuju)MCP 连接凭证
#
# 来源:金数据开放平台 > API Key(企业版套餐可生成)
#   文档:https://open.jinshuju.net/mcp/
#
# 连接以 HTTP Basic 方式认证:
#   Authorization: Basic base64("<JINSHUJU_API_KEY>:<JINSHUJU_API_SECRET>")
# agent/connections/jinshuju.ts 会自动读取下面两个明文值并完成 base64 编码,
# 你只需填入原始 key / secret,不要自己预先编码。
JINSHUJU_API_KEY=
JINSHUJU_API_SECRET=

# 说明:
# - 这是 app-scoped(应用级)共享凭证,agent 以这一个金数据账号的身份操作所有会话。
# - 若没有企业版,或需要「每个终端用户各自授权自己的金数据账号」,
#   请改用 OAuth 方式(https://open.jinshuju.net/mcp/oauth/),
#   并把 agent/connections/jinshuju.ts 中的 headers 认证替换为
#   Vercel Connect 或自定义 interactive authorization。
#
# 提醒:.env* 已被 .gitignore 忽略,真实凭证不会进入版本库,请勿提交真实值。
